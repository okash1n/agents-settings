interface:
  display_name: "OK Git"
  short_description: "Safe daily Git workflow with global identity"
  default_prompt: "Use $ok-git to inspect, branch, commit, and sync while keeping git config unchanged. Resolve the bundled helper from agent skills directories and use its absolute path; do not assume the working repository has scripts/git_ops.sh."
